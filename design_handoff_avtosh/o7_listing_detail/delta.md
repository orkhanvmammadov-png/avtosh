# Before → After (Direction 1A)
## Gallery
BEFORE: single fixed image block + small thumbs on light ground. AFTER: dominant 16:10 hero on the navy stage, dark thumb rail with selected-outline and "+n" tile, swipe-first mobile, fullscreen layer.
## Identity / price
BEFORE: title+price mixed into content column. AFTER: dedicated dark identity panel (400/340) — badges, price 34 Condensed, Kredit/Barter chips, title, meta — always beside the gallery at ≥1024.
## CTA
BEFORE: button within content flow. AFTER: green reveal CTA anchored in the panel (h48), full-width board CTA @768, sticky bar @390 (48px, safe-area, 72px offset), removed for SOLD/EXPIRED.
## Key specs
BEFORE: long flat attribute table. AFTER: 6 scannable tiles above grouped AVTOMOBİL/TEXNİKİ tables with hidden empty rows.
## Lower information
BEFORE: single monotone column. AFTER: 2-col paper panels — specs | condition+features+description — with expanders.
## Seller / contact
BEFORE: plain text block. AFTER: compact module (initial avatar, name, type·city) inside the panel; report as quiet footer text.
## Statuses
BEFORE: generic banner. AFTER: on-hero status chip + dedicated status board replacing CTA; dimmed price; no misleading contact.
## Mobile
BEFORE: shrunk desktop. AFTER: purpose-built 390 flow: swipe gallery → dark identity block → paper content → sticky contact bar.